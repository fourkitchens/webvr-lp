import Typography from "typography";

const typography = new Typography({
  baseFontSize: "18px",
  headerFontFamily: ['Whitney a', 'Whitney b', 'Helvetica Neue', 'Helvetica', 'Arial', 'sans-serif'],
  bodyFontFamily: ['Whitney a', 'Whitney b', 'Helvetica Neue', 'Helvetica', 'Arial', 'sans-serif'],
});

export default typography;
